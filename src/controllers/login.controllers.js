export const loginUser = (req, res) => {
    // try {
    //     const resp = getItem(parseInt(req.params.id))
    //     res.status(200).json(resp)

    // } catch (err) {
    //     res.status(500).send(err)
    // }
    res.send('Login User is working')
}